package com.shape;

public interface Polygon {
	void calcArea(float side);
	void calcPeri(float side);
	void calcArea(float length,float breadth);
	void calcPeri(float length,float breadth);

}